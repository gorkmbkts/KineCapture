"""Lucide icons, shipped verbatim.

Source: ``lucide-static`` 0.462.0, ISC licensed. ``LICENSE.lucide.txt`` is the
upstream licence text, copied unchanged. The SVG files are unmodified; the only
thing done to them at runtime is substituting ``currentColor`` for a theme
token so the same file works on both themes.

One set, one stroke weight, no emoji anywhere in the product.
"""
